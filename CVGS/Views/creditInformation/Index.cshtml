@model IEnumerable<CVGS.creditInformation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cardHolder)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.expiryDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cardType1.type)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cardHolder)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.expiryDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cardType1.type)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.cardNumber }) |
            @Html.ActionLink("Details", "Details", new { id=item.cardNumber }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.cardNumber })
        </td>
    </tr>
}

</table>
